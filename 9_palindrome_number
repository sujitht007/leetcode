bool isPalindrome(int x) {
    
    if (x < 0) return false;

    int rem = 0, a;
    int x1 = x;

    while (x != 0) {
        a = x % 10;


        if (rem > (INT_MAX - a) / 10) {
            return false;
        }

        rem = rem * 10 + a;
        x = x / 10;
    }

    return rem == x1;
}
